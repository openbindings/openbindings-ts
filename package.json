{
  "private": true,
  "packageManager": "pnpm@10.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "vitest run",
    "conformance": "pnpm --filter @openbindings/sdk conformance",
    "lint": "pnpm -r --filter './packages/*' exec tsc --noEmit"
  },
  "devDependencies": {
    "@types/json-schema": "^7.0.15",
    "@types/node": "^22.19.17",
    "tsup": "^8.5.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vite": "^8.1.3",
    "vitest": "^4.1.3"
  },
  "pnpm": {
    "overrides": {
      "vite": "^8.1.3",
      "esbuild": ">=0.28.1",
      "ip-address": ">=10.1.1"
    }
  }
}
