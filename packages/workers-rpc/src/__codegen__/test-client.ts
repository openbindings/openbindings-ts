// Auto-generated by ob codegen — do not edit
// Requires @openbindings/sdk ^0.2.0 (the OperationSignature/invoke API).
import { operationSignature } from "@openbindings/sdk";

// --- Types ---

export interface AddItemInput {
  name: string;
  qty?: number;
}

export interface AddItemOutput {
  id?: string;
  ok?: boolean;
}

export interface PingInput {
  message?: string;
}

export interface PingOutput {
  echoed: string;
}

// --- Operation signatures ---

/**
 * OperationSignatures holds one typed signature per operation declared by
 * the codegen-time OBI. Pass the runtime interface and a signature to
 * `invoker.invoke`; input flows through the handle's `write` and output
 * through `single` (the typed I/O is not a unary in/out shortcut):
 * 
 *     const call = invoker.invoke(obi, OperationSignatures.addItem);
 *     const out = await single(call.outputs);
 */
export const OperationSignatures = {
  /**
   * Add an item to the store
   */
  addItem: operationSignature<AddItemInput, AddItemOutput>("addItem"),
  /**
   * Health check
   */
  ping: operationSignature<PingInput, PingOutput>("ping"),
} as const;
